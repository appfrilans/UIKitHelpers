import UIKit

extension UIActivityIndicatorView {
    func setInProgress(_ inProgress: Bool) {
        if inProgress {
            startAnimating()
        } else {
            stopAnimating()
        }
    }
}
